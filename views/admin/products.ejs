<%- include('header') -%>
    <div class="admin-content-container">
        <h2 class="admin-heading">All Products</h2>
        <a class="add-new pull-right" href="add-product">Add New</a>
        <%
        if(products.length > 0) { %>
            <table id="productsTable" class="table table-striped table-hover table-bordered">
                <thead>
                    <th>Sr. No.</th>
                    <th>Title</th>
                    <th>Category</th>
                    <th>Sub-Category</th>
                    <th>Brand</th>
                    <th>Price</th>
                    <th>Quantity</th>
                    <th>Image</th>
                    <th>Status</th>
                    <th width="100px">Action</th>
                </thead>
                <tbody>
                    <% var sr_no = 1; %>
                    <% products.forEach(function(product){  %>
                    <tr>
                        <td><b><%-sr_no %></b></td>
                        <td><%- product.product_name %></td>
                        <td><%- product.product_category.category_name %></td>
                        <td><%- product.product_sub_cat.sub_category %></td>
                        <td><%- product.product_brand.brand_name %></td>
                        <td><%- product.product_price %></td>
                        <td><%- product.product_qty %></td>
                        <td>
                            <?php  if($row['featured_image'] != ''){ ?>
                                <img src="../product-images/<?php echo $row['featured_image']; ?>" alt="<?php echo $row['featured_image']; ?>" width="50px"/>
                            <?php }else{ ?>
                                <img src="images/index.png" alt="" width="50px"/>
                            <?php } ?>
                        </td>
                        <td><%
                                if(product.status == 1){ %>
                                    <span class="label label-success">Active</span>
                             <%   }else{ %>
                                    <span class="label label-danger">Inactive</span>
                              <%  }  %>
                        </td>
                        <td>
                            <a href="edit-product?id=<%- product._id %>"><i class="fa fa-edit"></i></a>
                            <a class="delete-product" href="javascript:void(0)" data-id="<%- product._id %>" data-subcat="<?php echo $row['product_sub_cat'] ?>"><i class="fa fa-trash"></i></a>
                        </td>
                    </tr>
                <% sr_no++ }) %>
                </tbody>
            </table>
        <% }else{ %>
            <div class="not-found clearfix">!!! No Products Found !!!</div>
        <%    } %>
        <div class="pagination-outer">
        <?php   //show pagination
        echo $db->pagination('products','sub_categories ON products.product_sub_cat=sub_categories.sub_cat_id LEFT JOIN brands ON products.product_brand=brands.brand_id',null,$limit);
        ?>
        </div>
    </div>
    <%- include('footer') -%>
