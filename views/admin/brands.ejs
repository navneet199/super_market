<%- include("header") -%>
<div class="admin-content-container">
    <h2 class="admin-heading">All Brands</h2>
    <a class="add-new pull-right" href="add-brand">Add New</a>
   <% if(brands.length){ %>
        <table class="table table-striped table-hover table-bordered">
            <thead>
            <th style="width: 35%;">Title</th>
            <th style="width: 45%;">Creation Time</th>
            <th>Action</th>
            </thead>
            <tbody>
            <% brands.forEach(function(item){   %>
                <tr>
                    <td style="width: 35%;text-transform: capitalize;"><%-  item.brand_name %></td>
                    <td style="width: 45%;"><%-  item.created.toLocaleString("nl-NL",{year:'numeric',
                        month: '2-digit' , day: '2-digit' , hour: '2-digit', minute: '2-digit', second: '2-digit'}) %></td>
                    <td>
                        <a href="edit-brand?id=<%- item._id  %>"><i class="fa fa-edit"></i></a>
                        <a class="delete_brand" href="javascript:void(0);" data-id="<%- item._id  %>" style="margin-left:15px;"><i class="fa fa-trash"></i></a>
                    </td>
                </tr>
            <% }) %>
            </tbody>
        </table>
    <%  }else{ %>
        <div class="not-found">!!! No Barnds Found !!!</div>
    <%   } %>
    <div class="pagination-outer">
        <?php echo $db->pagination('brands','categories ON brands.brand_cat=categories.cat_id',null,$limit); ?>
    </div>
</div>
<%- include("footer") -%>
